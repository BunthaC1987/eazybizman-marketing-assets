# Eazybizman marketing assets

Public image hosting for the LinkedIn/Facebook/Instagram post queue drafted by
the Marketing Brain vault's Wren agent. Used as the `assets` image URL source
for Buffer API scheduled posts — Buffer's API requires a publicly fetchable
URL rather than a file upload.

Source of truth for captions/hashtags: the vault's `Outputs/Wren/2026-08-28.md`.
